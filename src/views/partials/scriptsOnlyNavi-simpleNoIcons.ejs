<script>
    //Importante las notas sobre el tableStyle y el tableBox
    let estadoColorHead;
    const mainTheme = document.querySelector('.mainTheme'); 
    const controlHead = document.querySelector('.controlHead');
    const navHead = document.getElementById('navHead');
    const logoBlissenet = document.getElementById('logoBlissenet');
    const logoBlissenetCom = document.getElementById('logoBlissenet_com');
    const tableStyle = document.querySelector('.tableStyle');//se usa cuando solo hay una tabla en el html y se usa en una tabla
    const tableBox = document.querySelectorAll('.tableBox');//se usara cuando hay mas de una table en un HTML igual su uso es en una tabla
    const bodyTheme = document.querySelectorAll('.bodyTheme');
    const themaTextLight = document.querySelectorAll('.themaTextLight'); //esta clase para que funcione debe tener en el style del html la clase .textLight
    const linkOptions = document.querySelectorAll('.linkOptions');
    const showMessage = document.querySelector('.showMessage');
    const contentMessage = document.querySelector('.contentMessage');

  
    hColor()
  
    function hColor(){
      estadoColorsHead = localStorage.getItem("estadoHead");
  
      console.log("valor de estadoHead", estadoColorsHead)
      if (estadoColorsHead === 'light'){
        mainTheme.classList.remove('bg-dark');
        navHead.style.backgroundColor = "#f9f9f9";
        navHead.classList.add('navbar-light');
        logoBlissenet.innerHTML = `<img src="/img/logoBlissenetJonathan.png" style="width: 40px;"> <span> Blissenet  <span class="countryNav ms-1" style="font-size: 12px">  </span> </span> `
        if (logoBlissenetCom){
          logoBlissenetCom.innerHTML = `<img src="/img/logoBlissenetJonathan.png" style="width: 40px;"> <strong style="font-size: 20px"> Blissenet.com <strong>`;
        }
        if (tableStyle){
          tableStyle.classList.remove('table-dark');
        }
        controlHead.innerHTML = '<img src="/img/iconoMoon.png" class="me-1"  style="width: 24px;">'
        tableBox.forEach((ele)=>{
          ele.classList.remove('table-dark');
        })
        if (bodyTheme){
          bodyTheme.forEach((ele)=>{
            ele.classList.remove('blackBody');
          })
        }
        if (themaTextLight){
          themaTextLight.forEach((ele)=>{
            ele.classList.remove("textLight")
          })
        }
        if (linkOptions){
          linkOptions.forEach((ele)=>{
            ele.style.color = "black";
          })
        }
        
        if (contentMessage){
          contentMessage.style.backgroundColor = "white";
          showMessage.style.backgroundColor = "white";
        }  
        
      } else {
        mainTheme.classList.add('bg-dark');
        navHead.style.backgroundColor = "rgb(27, 27, 27)";
        navHead.classList.add('navbar-dark');
        logoBlissenet.innerHTML = `<img src="/img/logoBlissenetJonathan.png" style="width: 40px;"> <span> Blissenet  <span class="countryNav ms-1" style="font-size: 12px">  </span> </span> `
        if (logoBlissenetCom){
          logoBlissenetCom.innerHTML = `<img src="/img/logoBlissenetJonathan.png" style="width: 40px;"> <strong style="font-size: 20px"> Blissenet.com <strong>`;
        }
        if (tableStyle){
          tableStyle.classList.add('table-dark');
        }
        controlHead.innerHTML = '<img src="/img/iconoSun.png" class="me-1"  style="width: 24px;">'
        tableBox.forEach((ele, i)=>{
          ele.classList.add('table-dark');
        })
        if (bodyTheme){
          bodyTheme.forEach((ele)=>{
            ele.classList.add('blackBody');
          });  
        }
        if (themaTextLight){
          themaTextLight.forEach((ele)=>{
            ele.classList.add("textLight")
          })
        }
        if (linkOptions){
          linkOptions.forEach((ele)=>{
            ele.style.color = "white";
          })
        }
       
        if (contentMessage){
          contentMessage.style.backgroundColor = "#1B1B1B";
          showMessage.style.backgroundColor = "#1B1B1B";
        }  

          
      }
  
    }
  
  
    controlHead.addEventListener('click', ()=>{
  
  
     estadoColorHead = localStorage.getItem("estadoHead");
  
     if (estadoColorHead === "light"){
      console.log("el estado es light y debo cambiarlo a dark ");
      mainTheme.classList.add('bg-dark');
      navHead.style.backgroundColor = "rgb(27, 27, 27)";
      navHead.classList.add('navbar-dark');
      navHead.classList.remove('navbar-light');
      logoBlissenet.innerHTML = `<img src="/img/logoBlissenetJonathan.png" style="width: 40px;"> <span> Blissenet  <span class="countryNav ms-1" style="font-size: 12px">  </span> </span> `
      if (logoBlissenetCom){
        logoBlissenetCom.innerHTML = `<img src="/img/logoBlissenetJonathan.png" style="width: 40px;"> <strong style="font-size: 20px"> Blissenet.com <strong>`;
      }
      if (tableStyle){
         tableStyle.classList.add('table-dark');
      }
      controlHead.innerHTML = '<img src="/img/iconoSun.png" class="me-1"  style="width: 24px;">'
      tableBox.forEach((ele, i)=>{
          ele.classList.add('table-dark');
      })
      if (bodyTheme){
        bodyTheme.forEach((ele)=>{  
          ele.classList.add('blackBody');
        })  
      }
      if (themaTextLight){
          themaTextLight.forEach((ele)=>{
            ele.classList.add("textLight")
          })
      }
      if (linkOptions){
          linkOptions.forEach((ele)=>{
            ele.style.color = "white";
          })
      }
     
      if (contentMessage){
        contentMessage.style.backgroundColor = "#1B1B1B";
        showMessage.style.backgroundColor = "#1B1B1B";
      } 


      localStorage.setItem("estadoHead", "dark" );
      estadoTheme = localStorage.getItem('theme');
            
     } else {
      console.log("el estado es dark y debo cambiarlo a light ");
      mainTheme.classList.remove('bg-dark');
      navHead.style.backgroundColor = "#f9f9f9";
      navHead.classList.add('navbar-light');
      navHead.classList.remove('navbar-dark');
      logoBlissenet.innerHTML = `<img src="/img/logoBlissenetJonathan.png" style="width: 40px;"> <span> Blissenet  <span class="countryNav ms-1" style="font-size: 12px">  </span> </span> `
      if (logoBlissenetCom){
        logoBlissenetCom.innerHTML = `<img src="/img/logoBlissenetJonathan.png" style="width: 40px;"> <strong style="font-size: 20px"> Blissenet.com <strong>`;
      }
      if (tableStyle){
          tableStyle.classList.remove('table-dark');
      }
      controlHead.innerHTML = '<img src="/img/iconoMoon.png" class="me-1"  style="width: 24px;">'
      tableBox.forEach((ele, i)=>{
          ele.classList.remove('table-dark');
      })
      if (bodyTheme){
        bodyTheme.forEach((ele)=>{
          ele.classList.remove('blackBody');
        })  
      }
      if (themaTextLight){
          themaTextLight.forEach((ele)=>{
            ele.classList.remove("textLight")
          })
      }
      if (linkOptions){
          linkOptions.forEach((ele)=>{
            ele.style.color = "black";
          })
      }
      
      if (contentMessage){
        contentMessage.style.backgroundColor = "white";
        showMessage.style.backgroundColor = "white";
      }


      localStorage.setItem("estadoHead", "light" )
      estadoTheme = localStorage.getItem('theme');
          
     }
  
     console.log("el estado es", estadoColorHead);
  
    });
  
  
    //este script coloca el codigo de pais arriba en el logo
    const seeMarket = document.getElementById('seeMarket').value;
    const countryNav = document.querySelector('.countryNav');

    console.log("seeMarket ----->", seeMarket)

    if (seeMarket.length === 0) {
      //el usuario no tiene un seemarket definido. o no tiene un usuario
    
    } else {

      //el usuario ya tiene un usuario definido.
      console.log("el usuario ya tiene un usuario definido.")
      console.log("seeMarket --------> ojo", seeMarket)
      console.log("countryNav", countryNav)
      countryNav.innerText = seeMarket;

    } 

   </script>

